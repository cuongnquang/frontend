export enum AppointmentStatus {
    Pending = "pending",
    Confirmed = "confirmed",
    Completed = "completed",
    Cancelled = "cancelled",
    NoShow = "no_show"
}

export enum Role {
    ADMIN = "admin",
    DOCTOR = "doctor",
    PATIENT = "patient",
}

export enum Gender {
    MALE = "male",
    FEMALE = 'female',
    OTHER = 'other'
}