export enum Role {
  PATIENT = "patient",
  DOCTOR = "doctor",
  ADMIN = "admin",
  HOSPITAL = 'hospital'
}

export enum Gender {
  MALE = "male",
  FEMALE = "female",
  OTHER = "other",
}

export enum AppointmentStatus {
  PENDING = "pending",
  CANCELLED = "cancelled",
  COMPLETED = "completed",
}
